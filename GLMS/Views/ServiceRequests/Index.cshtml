 @model IEnumerable<GLMS.ApiClient.ServiceRequestDto>

@{
    ViewData["Title"] = "Sevice Requests";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Contract</th>
            <th>Client</th>
            <th>
                @Html.DisplayNameFor(model => model.Discription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CostUSD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CostZAR)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExchangeRateUsed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @item.ContractServiceLevel
            </td>
            <td>
                @item.ClientName
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CostUSD)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CostZAR)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExchangeRateUsed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<p class="text-muted small mt-3">
    <a href="https://www.exchangerate-api.com" target="_blank" rel="noopener">Rates By Exchange Rate API</a>
</p>
